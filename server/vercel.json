{
    "version": 2,
    "builds": [
      {
        "src": "dist/main.ts",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      { "handle": "filesystem" },
      { "src": "/(.*)", "dest": "/dist/main.ts", "methods": ["GET", "POST", "PUT", "DELETE"] }
    ]
}